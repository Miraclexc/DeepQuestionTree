/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false, // React Flow recommends turning this off if double-rendering issues occur, though 18 handles it better.
}

module.exports = nextConfig
